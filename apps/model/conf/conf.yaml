server:
  name: model
  address: ":8935"

log:
  level: debug
  file_name: deploy/log/model.log

config_center:
  port: 8500

registry:
  address:
    - ":8500"

db:
  dsn: "root:123456@tcp(127.0.0.1:3306)/aiagt?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  address: "127.0.0.1:6379"

metrics:
  addr: ":9935"

tracing:
  export_addr: "localhost:4317"
