server:
  name: gateway
  address: ":8930"

log:
  level: debug
  file_name: deploy/log/gateway.log

config_center:
  port: 8500

registry:
  address:
    - ":8500"

db:
  dsn: "root:123456@tcp(127.0.0.1:3306)/aiagt?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  address: "127.0.0.1:6379"

metrics:
  addr: ":9930"

tracing:
  export_addr: "localhost:4317"

cos:
  url: "xxx"
  secret_id: "xxx"
  secret_key: "xxx"
